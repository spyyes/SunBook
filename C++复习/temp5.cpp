继承：A基类 B派生类（子类）
避免重复的代码造成浪费，先写一个基类，代表共同特点；然后派生出各子类，
class Student{

};
class UndergraduateStudent:public Student{

};

派生类对象的体积：基类对象存储位置：在派生类对象之前

派生类和基类的同名同参数表的成员函数： 叫做覆盖
或者是同名的成员变量
如果要调用基类中的成员函数，需要明确指出类修饰符

派生类和基类的各种权限：重要 public private protected
3种继承方式

区分集成 和复合

构造函数

有成员对象和基类的时候的构造函数执行顺序：
基类--成员对象--本身

派生类对象可以赋值给基类指针，但是不能用这个指针访问派生类对象的独有成员
但是可以用强制类型转换再转成派生类指针，但是程序员应该注意安全